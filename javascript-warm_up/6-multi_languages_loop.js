#!/usr/bin/node

// Store the sentences in an array
const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Use a standard 'for' loop to go through each item in the array
// i starts at 0 (the first index). The loop runs as long as i is less than the array's length.
for (let i = 0; i < languages.length; i++) {
  console.log(languages[i]);
}
